{% extends 'base.html.twig' %}

{% block title %}Hello AdminController!
{% endblock %}

{% block body %}
	<head>
		{{ vite_entry_link_tags('assets_poc') }}
	</head>
	<span>
		TS asset loaded in Twig:
		{# run npm run build and see that it uses filename with hash #}
		{# this works due to mapping in public/build/.vite/manifest.json that is generated on npm run build #}
	<img src="{{ asset('src/assets/react.svg') }}"> </span>
	<span>
		Twig asset loaded in Twig:
		<img src="images/symfony.png">
	</span>
	<div id="root"></div>
	{{ vite_entry_script_tags('assets_poc', { dependency: 'react' }) }}
{% endblock %}
